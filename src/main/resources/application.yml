spring:
  datasource:
    driver-class-name:
    url:
    username:
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQL5Dialect
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
EventContractAddress:
BlockchainPath:

server:
  port: 9191